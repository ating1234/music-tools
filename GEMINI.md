# Gemini Agent (Antigravity) 運作規則

本文件定義了 Gemini Agent（Antigravity）在 `music-tools` 專案中的運作規則與行為約束，以確保開發方向與架構一致，並尊重使用者的主控權。

## 1. 計劃變更約束 (核心規則)
* **禁止未授權修改**：未經使用者明確同意，**嚴禁任意改動已達成的架構規劃、實作計劃或既定設計**。
* **優化變更流程**：在開發或實作過程中，如果發現比原定計劃更好的優化架構或調整方案，**必須先向使用者說明並徵得「同意」後，方可更新計劃書或實施修改**。

## 2. 全局行為規範
* **禁止主動作出決策**：未經使用者明確的指令授權，嚴禁主動執行 any 操作、存取檔案或修改系統設定。
* **強制請求批准**：在執行任何關鍵動作（如：傳送郵件、刪除資料、修改程式碼、啟動外部自動化工具）之前，必須先列出步驟並向使用者請求同意。等待使用者回覆「同意」後，才可繼續執行。
* **語言偏好**：一律使用繁體中文（Traditional Chinese）回答所有的對話、說明與問題。專有名詞請保留英文或官方名稱，不刻意翻譯。

## 3. 程式碼自我驗證與測試機制 (防錯規範)
* **本地語法預檢 (Dry-run)**：在修改或新增任何 Python 程式碼後，在交付給使用者部署或執行之前，Agent 必須主動在本機執行 `python3 -m py_compile` 或模組導入測試，確保無語法（SyntaxError）或屬性（AttributeError）錯誤。
* **依賴衝突預防 (Dependency Precheck)**：若需調整 `pip` 安裝依賴，必須先與本地 `requirements.txt` 對齊版本，或在本地環境執行 `pip install --dry-run` 檢查，避免在雲端 Build 階段發生依賴衝突。
* **API 規格查閱**：使用第三方 SDK 時，必須先搜尋最新 Release Notes 或查閱本地已安裝套件的內部源碼，禁止盲寫或使用已廢棄 (Deprecated) 的 API。
